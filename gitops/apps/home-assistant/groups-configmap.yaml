apiVersion: v1
kind: ConfigMap
metadata:
  name: homeassistant-groups
data:
  groups.yaml: |
    guests:
      name: Guests
      entities:
        - device_tracker.brayan_iphone
      all: false
    
