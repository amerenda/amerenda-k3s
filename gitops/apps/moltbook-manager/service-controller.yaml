apiVersion: v1
kind: Service
metadata:
  name: moltbook-controller
  namespace: moltbook-manager
spec:
  type: ClusterIP
  selector:
    app: moltbook-controller
  ports:
    - name: http
      port: 8082
      targetPort: 8082
